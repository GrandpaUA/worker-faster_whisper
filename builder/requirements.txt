runpod~=1.9.0

faster-whisper==1.2.1

# Demucs separation (GPU). torch/torchaudio з CUDA wheels — база nvidia/cuda 12.6,
# cu126 індекс гарантує CUDA-сумісні колеса.
--extra-index-url https://download.pytorch.org/whl/cu126
torch
torchaudio
demucs

# Roformer separation (nomadkaraoke/python-audio-separator). [gpu] додає
# onnxruntime-gpu (CUDA-ліби бере з базового nvidia/cuda образу), torch уже
# стоїть вище з cu126. Пін версії: реєстр моделей і API пакета змінюються
# між релізами.
audio-separator[gpu]==0.44.5
